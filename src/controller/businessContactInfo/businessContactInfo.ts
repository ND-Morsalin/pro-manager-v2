import { Response } from "express";
import { ExtendedRequest } from "../../types/types";

const createBusinessContactInfo = async (req:ExtendedRequest,res:Response)=>{
    try {
        
    } catch (error) {
        
    }
}